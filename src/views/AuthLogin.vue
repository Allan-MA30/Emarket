<template>
  <div class="page-wrapper">
    <!--
    ============================================================
    LOGIN PAGE — Assigned to: [Teammate Name]
    ============================================================
    What to build here:
    - Login form: email, password
    - On submit: auth.login({ id, name, email, role })
    - Redirect to /dashboard if seller, /property/buy if viewer

    Import:
      import { useAuthStore } from '@/stores/auth'
      const auth = useAuthStore()
    ============================================================
    -->
    <h1 style="color: var(--text-muted); text-align:center; margin-top: 4rem;">
      🔐 Login page — coming soon (assigned teammate)
    </h1>
  </div>
</template>

<script setup>
// TODO: implement Login page
</script>
