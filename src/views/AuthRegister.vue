<template>
  <div class="page-wrapper">
    <!--
    ============================================================
    REGISTER PAGE — Assigned to: [Teammate Name]
    ============================================================
    What to build here:
    - Registration form: name, email, password
    - Role selector: "Viewer / Lendee" OR "Seller"
    - On submit, call: auth.register({ name, email, role })
    - Redirect to /dashboard if seller, or /property/buy if viewer

    Import:
      import { useAuthStore } from '@/stores/auth'
      const auth = useAuthStore()
      auth.register({ id: 1, name: 'Jean', email: '...', role: 'seller' })
    ============================================================
    -->
    <h1 style="color: var(--text-muted); text-align:center; margin-top: 4rem;">
      📝 Register page — coming soon (assigned teammate)
    </h1>
  </div>
</template>

<script setup>
// TODO: implement Register page
</script>
